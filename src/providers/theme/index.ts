import { TTheme } from './types';

const colors = {
  base: '#c1c1c1',
  configuration: '#39b2c2',
};

const theme: TTheme = {
  colors,
  boxShadow: '0 1px 4px -1px #808080',
};

export default theme;
